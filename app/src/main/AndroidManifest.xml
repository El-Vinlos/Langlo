<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LangloDefaultTheme">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.deck.DeckActivity"
            android:label="Decks"
            android:exported="false" />


        <activity
            android:name=".ui.cardstudy.CardStudyActivity"
            android:exported="false" />

        <activity android:name=".ui.account.LoginActivity"
            android:exported="false"/>

        <activity android:name=".ui.account.AccountActivity"
            android:exported="false"/>

        <activity android:name=".ui.speech.SpeakActivity"
            android:exported="false"/>

        <activity android:name=".ui.exam.ExamActivity"
            android:exported="false"/>

        <activity android:name=".ui.exam.ExamSelectorActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.speech.PronunciationTestActivity"
            android:exported="false"
            android:parentActivityName=".ui.speech.SpeakActivity" />
    </application>

</manifest>